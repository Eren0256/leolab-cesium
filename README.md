# LEOLab Cesium Viewer

A lightweight Cesium viewer for LEOLab CZML outputs.

## Usage

```bash
npm install
npm run dev
```

Then open the local URL printed by Vite.

Large generated CZML files should stay local under `public/czml/`. Only the demo
CZML file is tracked by Git.
